language: python
python:
  - "2.6"
  - "2.7"
  - "3.6"
# command to install dependencies
install:
  - "pip install mock pytest"
  - "pip install -e ."
# command to run tests
script:
  - "py.test -rxs -rf"
